# pydoc-markdown_tio_protected.yml
loaders:
  - type: python
    # Where to search for Python packages/modules
    search_path:
      - ./../tableio_cfg_json/src
    # Packages (directories with __init__.py) to document
    packages:
      - tableio_cfg_json

processors:
  - type: filter
    # Optional: skip private members, etc.
    skip_empty_modules: true
    exclude_private: false
  - type: smart
  - type: crossref

renderer:
  type: markdown
  filename: ./doc/tableio_cfg_json_protected_api.md
