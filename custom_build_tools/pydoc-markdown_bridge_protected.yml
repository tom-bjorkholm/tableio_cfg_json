# pydoc-markdown.yml
loaders:
  - type: python
    # Where to search for Python packages/modules
    search_path:
      - ./../wizard_ui_bridge/src
    # Packages (directories with __init__.py) to document
    packages:
      - wizard_ui_bridge

processors:
  - type: filter
    # Optional: skip private members, etc.
    skip_empty_modules: true
    exclude_private: false
  - type: smart
  - type: crossref

renderer:
  type: markdown
  filename: ./doc/wizard_ui_bridge_protected_api.md
